# useControlledValue Hook

## 题型定位

受控 / 非受控组件状态合并题。

## 核心思路

先判断外部是否传入 `value`，传入则以外部值为准；未传入则维护内部状态，并在更新时统一触发 `onChange`。

## 标准实现

待补。

## 口述模板

待补。

## 边界情况

- `value` 为 `undefined` 时如何判断非受控。
- `defaultValue` 只在初始化时生效。
- `onChange` 的触发时机。

## 变体

- 支持函数式更新。
- 支持受控和非受控切换时的告警。

## 错点

待复述后补。

## 来源

tracker 初始化条目。

