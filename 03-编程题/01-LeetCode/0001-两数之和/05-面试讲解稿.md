# 面试讲解稿

## 题目复述（10 秒）

在数组里找两个数，它们的和等于 target，返回下标。

## MVP 思路（30 秒）

遍历数组，用哈希表记录“某个数出现过的下标”。当前数是 x，就看 target-x 是否出现过，出现过就直接返回。

## 加分点（可讲）

- 为什么不能用双重循环：O(n^2) 在数据大时不稳
- 如何处理重复值：哈希表保存第一次出现的位置即可
- 如果题意可能无解：返回 null 或抛错并解释取舍
