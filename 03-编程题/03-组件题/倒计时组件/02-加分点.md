# 加分点（按收益排序）

1) 格式化展示  
- 输出 `mm:ss` 或 `hh:mm:ss`，并补齐前导 0

2) 精度与性能  
- 用 requestAnimationFrame 或动态 interval，避免 250ms 固定轮询
- 只在秒变化时 setState，减少重渲染

3) 可测试性  
- 抽出“计算剩余时间”的纯函数
- 支持注入 `now()`，便于单测

4) 边界处理  
- targetMs 变化时重置
- onFinish 只触发一次
