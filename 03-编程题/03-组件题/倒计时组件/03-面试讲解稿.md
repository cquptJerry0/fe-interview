# 面试讲解稿

## MVP（面试最快版本）

核心点就两件事：
- state 存当前时间 now
- 通过 targetMs-now 计算剩余秒数，并用定时器刷新 now

## 我会怎么讲加分点

- 展示层：把秒数格式化成 `mm:ss`，这是用户体验的直接提升
- 性能：只在秒变化时更新，避免不必要的重渲染
- 可测试：把“剩余时间计算”抽成纯函数，now 可注入，单测就稳定
- 边界：target 变化、组件卸载、onFinish 触发时机都要兜住
