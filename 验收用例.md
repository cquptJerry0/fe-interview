# 验收用例（最小端到端）

## 用例 1：批量面经 + 编程题

输入（粘贴给总控 Agent）：

1. 事件循环里微任务/宏任务怎么跑？怎么验证执行顺序？
2. HTTP 缓存强缓存/协商缓存是什么？线上怎么排查缓存没生效？
3. CSS 方案怎么选？多主题怎么做？Tailwind 变量怎么映射到页面？

编程题：
- 实现一个倒计时组件（输入 targetMs，结束触发 onFinish）

画像（可选）：
- 贴 `05-训练记录/01-熟练度画像/profile.yaml` 片段

期望输出：

1) 先输出结构化题目清单（含类型、难度、tags、优先级）  
2) 对每题给出：用户先答（若用户愿意）→ 双评分 → 改写满分回答 → 追问脚本  
3) 编程题给出：MVP（核心逻辑可跑）→ 加分点清单（收益排序）  
4) 模块结束给出归档：unknown/new_learned/need_review/mastered（按 tags）  
5) 给出画像更新建议（tag 维度）

验收通过标准：
- 输出结构稳定，可直接复制到 `05-训练记录/02-训练会话/`
- 每题至少包含 1 组对比与 trade-off
- 不会的点能触发“例子+图解+费曼复述+递进追问”
